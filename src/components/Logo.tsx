/** @jsxImportSource @emotion/react */
import React from 'react';

import logo from '../assets/cellNowLogo.png';

const Logo = (): React.ReactElement => {
  return <img css={{ height: '100%' }} src={logo} alt="A text and logo of the company" />;
};

export default Logo;
